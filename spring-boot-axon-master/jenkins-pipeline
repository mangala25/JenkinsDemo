stage('dev'){
        echo 'dev build runs'
    }
stage('stag'){
	fileExists 'spring-boot-axon-master/pom1.xml'
}
stage('pre-prod'){
	echo 'pe-prod build runs'
}
stage('prod'){
	echo 'prod build runs'
}