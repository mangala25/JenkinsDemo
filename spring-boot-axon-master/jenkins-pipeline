stage('dev'){
	echo 'dev build runs'
}
stage('stag'){
	node{
		fileExists 'spring-boot-axon-master/pom.xml'
	}
}
stage('pre-prod'){
	echo 'pe-prod build runs'
}
stage('prod'){
	echo 'prod build runs'
}