stage('dev'){
	echo 'dev build runs'
}
stage('stag'){
	node {
		def exists = fileExists 'spring-boot-axon-master\jenkins-pipeline'
		println exists
		if(exists){
			echo 'File found'
		} else{
			error 'File not found'
		}
		
		/*git init 
		git clone :https://github.com/mangala25/JenkinsDemo.git
		git branch d1
		git log*/
	}
}
stage('pre-prod'){
	echo 'pe-prod build runs'
}
stage('prod'){
	echo 'prod build runs'
}